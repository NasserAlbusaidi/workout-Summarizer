import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Interval Matcher | Match Planned Workouts with FIT File Data",
  description: "Privacy-first workout analysis tool. Compare your intervals.icu training plan with actual FIT file data. See how you performed against each planned interval.",
  keywords: "workout analysis, FIT file, intervals.icu, running, cycling, swimming, training, exercise",
};


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
